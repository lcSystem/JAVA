spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_erp_login5?createDatabaseIfNotExist=true&userSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC&jdbcCompliantTruncation=false
    username: root
    password: 123456
  jpa:
    hibernate:
       ddl-auto: validate
    showSql: true
    properties:
      hibernate:
        formatSql: true
    database: mysql
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false
    locations: classpath:db/migration
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mvc:
    static-path-pattern: /assets/**
  web:
    resources:
      static-locations: file:./src/main/resources/static/assets/
server:
  port: 8081

app:
  security:
    session-secret: "vF6eH9mN2pQ5rT8uW1xZ4aD7gJ0kM3pS" # Recommended to change in production
    session-limit: 5

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
